{
	int a, b, e;
	float c, d, bit_foda;
	String f, abc;

	print("Valor de A:");
	read(a);
	a = 200;
	print("Valor de B:");
	read(b);
	b = 100;
	while (a != b)
	{
		if (a > b)
		{
			a = a + b;
		}
		if (b > a)
		{
			b = b - a;
		}
	}
	c = 52.1;
	d = 30.7;
	e = b * 2 - 1 + a;
	bit_foda = c * d + (a + 1 - b) / e;
	print("\nRes=");
	print(a);
}
